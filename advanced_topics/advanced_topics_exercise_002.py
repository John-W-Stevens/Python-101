# Modify the following function to return dictionary of all the names from the list of names and the length
# of each name. For example, for an input of ['John', "Bob"] the output should be {'John': 4, 'Bob': 3}
# You must use a dictionary comprehension.

def get_dict_of_names_and_name_lengths(list_of_names):
    # Your code here
    return

result = get_dict_of_names_and_name_lengths(['John', 'Bob'])
assert result.get('John') == 4
assert result.get('Bob') == 3