# Complete the function below to return True if the input is a string that starts with the letter 'A'

def does_string_start_with_A(a_string):
    return False

assert does_string_start_with_A("A huge earthquake hit Baltimore!") == True
assert does_string_start_with_A("a man had lunch with his wife.") == False
assert does_string_start_with_A("Hello World!") == False